name: Docker Image CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: |
        echo "${{ secrets.DOCKERPW }}" | docker login -u "jestenok" --password-stdin 
        docker buildx . --file Dockerfile --tag jestenok/mysite:latest --platform linux/arm64
        docker push jestenok/mysite:latest
